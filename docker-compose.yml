version: "3"

services:
  db:
    container_name: django-db_v4.0.4
    image: postgres
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres

  web:
    container_name: django-db_v4.0.4
    build: .
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    tty: true
    depends_on:
      - db
    